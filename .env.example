# Vercel Analytics Configuration
# These environment variables are automatically provided by Vercel when deployed
# No additional configuration needed for basic analytics

# For development with custom analytics configuration (optional):
# VERCEL_ENV=development

# Future API Integration (when backend is ready)
# NEXT_PUBLIC_API_URL=http://localhost:3001/api
# NEXT_PUBLIC_GSTN_API_URL=https://api.gst.gov.in
# JWT_SECRET=your-secret-key
# DATABASE_URL=postgresql://username:password@localhost:5432/gst_db

# External Services (for production)
# REDIS_URL=redis://localhost:6379
# AWS_S3_BUCKET=gst-documents
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# Email & SMS (for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMS_API_KEY=your-sms-api-key